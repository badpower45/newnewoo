# 📊 دليل رفع المنتجات من Excel Sheet

## 📋 الأعمدة المطلوبة في ملف Excel

### ✅ أعمدة إلزامية (Required)
هذه الأعمدة **يجب** أن تكون موجودة في الملف:

| اسم العمود | Column Name | النوع | مثال | ملاحظات |
|------------|------------|-------|------|---------|
| اسم المنتج | `name` أو `product_name` | نص | "شوكولاتة جالاكسي" | **إلزامي** |
| السعر | `price` | رقم | 25.50 | **إلزامي** - بالجنيه المصري |
| الصورة | `image` أو `image_url` | رابط | https://... | **إلزامي** - رابط الصورة |
| القسم | `category` | نص | "حلويات" | **إلزامي** |

---

### 🔧 أعمدة اختيارية (Optional)
هذه الأعمدة **اختيارية** - إذا لم تكن موجودة، سيتم وضع `null`:

| اسم العمود | Column Name | النوع | مثال | القيمة الافتراضية |
|------------|------------|-------|------|--------------------|
| الاسم بالإنجليزية | `name_en` | نص | "Galaxy Chocolate" | `null` |
| الوصف | `description` | نص | "شوكولاتة لذيذة..." | `null` |
| الوصف بالإنجليزية | `description_en` | نص | "Delicious chocolate..." | `null` |
| الوزن | `weight` | نص | "100g" أو "1 كجم" | `null` |
| الباركود | `barcode` | نص | "6221155123456" | `null` |
| SKU | `sku` | نص | "CHOC-GAL-100" | `null` |
| البراند | `brand` أو `brand_id` | نص/رقم | "جالاكسي" أو 5 | `null` |
| الكمية المتاحة | `stock_quantity` | رقم | 100 | 0 |
| السعر القديم | `old_price` | رقم | 30.00 | `null` |
| نسبة الخصم | `discount_percentage` | رقم | 15 | 0 |
| معلومات التغذية | `nutrition_info` | JSON | {...} | `null` |
| المكونات | `ingredients` | نص | "حليب، سكر..." | `null` |
| الحساسية | `allergens` | نص | "حليب، فول سوداني" | `null` |
| معرف الفرع | `branch_id` | رقم | 1 | 1 (الفرع الافتراضي) |

---

## 📝 مثال ملف Excel

### صيغة بسيطة (الحد الأدنى):
```
name                    | price  | image                              | category
------------------------|--------|------------------------------------|---------
شوكولاتة جالاكسي        | 25.50  | https://i.imgur.com/abc123.jpg     | حلويات
بيبسي 2 لتر            | 18.00  | https://i.imgur.com/xyz789.jpg     | مشروبات
شيبسي ليز كبير         | 12.50  | https://i.imgur.com/def456.jpg     | سناكس
```

### صيغة كاملة (بجميع الأعمدة):
```
name          | name_en       | price | old_price | discount_percentage | category | weight | barcode       | sku          | brand   | stock_quantity | image                              | description              | description_en
--------------|---------------|-------|-----------|---------------------|----------|--------|---------------|--------------|---------|----------------|------------------------------------|--------------------------|---------------
شوكولاتة     | Galaxy Choc   | 25.50 | 30.00     | 15                  | حلويات   | 100g   | 6221155123456 | CHOC-GAL-100 | جالاكسي | 150            | https://i.imgur.com/abc123.jpg     | شوكولاتة لذيذة بالحليب  | Delicious...
بيبسي        | Pepsi 2L      | 18.00 |           | 0                   | مشروبات  | 2L     | 6221155789012 | PEP-2L       | بيبسي   | 200            | https://i.imgur.com/xyz789.jpg     | مشروب غازي منعش         | Refreshing...
```

---

## 🔄 كيفية رفع الملف

### الخطوات:

1. **تحضير الملف**:
   - افتح Excel أو Google Sheets
   - احفظ الملف بصيغة `.xlsx` أو `.csv`
   - تأكد من وجود الأعمدة الإلزامية الأربعة

2. **رفع الملف على السيستم**:
   ```
   انتقل إلى: لوحة التحكم → إدارة المنتجات → استيراد من Excel
   ```

3. **اختر الملف**:
   - اضغط "اختر ملف" واختر الملف من جهازك
   - سيظهر لك معاينة للبيانات

4. **تأكيد الاستيراد**:
   - راجع البيانات
   - اضغط "استيراد المنتجات"
   - سيتم إضافة جميع المنتجات للنظام

---

## ⚙️ معالجة البيانات المفقودة

### القواعد التلقائية:
- **الأعمدة المفقودة تماماً**: سيتم وضع `null`
- **الخلايا الفارغة**: سيتم وضع `null`
- **السعر القديم**: إذا كان فارغ، لن يظهر خصم
- **نسبة الخصم**: إذا كانت 0 أو فارغة، لن يظهر badge الخصم
- **الكمية**: إذا كانت فارغة، سيتم وضع 0 (نفذت الكمية)
- **الوزن**: إذا كان فارغ، لن يظهر الوزن على البطاقة

---

## 🚨 الأخطاء الشائعة وحلولها

### ❌ خطأ: "Missing required columns"
**السبب**: أحد الأعمدة الإلزامية مفقود  
**الحل**: تأكد من وجود: `name`, `price`, `image`, `category`

### ❌ خطأ: "Invalid price format"
**السبب**: السعر ليس رقماً  
**الحل**: استخدم أرقام فقط (مثال: 25.50 وليس "25.50 جنيه")

### ❌ خطأ: "Invalid image URL"
**السبب**: رابط الصورة غير صحيح  
**الحل**: استخدم روابط كاملة تبدأ بـ `https://`

### ❌ خطأ: "Category not found"
**السبب**: القسم غير موجود في النظام  
**الحل**: أضف القسم أولاً من إدارة الأقسام، أو استخدم أقسام موجودة

---

## 📸 رفع الصور

### خيارات رفع الصور:

#### 1️⃣ استخدام روابط خارجية (موصى به):
- ارفع الصور على Imgur أو Cloudinary
- انسخ الرابط المباشر للصورة
- ضعه في عمود `image`
- مثال: `https://i.imgur.com/abc123.jpg`

#### 2️⃣ استخدام Base64 (للصور الصغيرة):
- حول الصورة لـ Base64
- ضع الكود في عمود `image`
- مثال: `data:image/jpeg;base64,/9j/4AAQ...`

#### 3️⃣ الرفع اليدوي:
- ارفع الصور واحدة واحدة من صفحة تعديل المنتج
- يمكنك إضافة المنتج بدون صورة أولاً

---

## 🔢 التعامل مع الأرقام

### نصائح:

- **السعر**: استخدم نقطة (.) للفاصلة العشرية، مثال: `25.50`
- **الباركود**: رقم نصي (يمكن أن يبدأ بصفر)
- **الكمية**: عدد صحيح (integer)
- **نسبة الخصم**: رقم من 0 إلى 100

---

## 🎯 أمثلة تطبيقية

### مثال 1: منتج بسيط
```excel
name: شوكولاتة كيتكات
price: 5.00
image: https://i.imgur.com/kitkat.jpg
category: حلويات
```
✅ سيتم إضافة المنتج بنجاح، باقي الحقول ستكون `null`

### مثال 2: منتج بخصم
```excel
name: كوكاكولا 1.5 لتر
price: 12.00
old_price: 15.00
discount_percentage: 20
image: https://i.imgur.com/coca.jpg
category: مشروبات
weight: 1.5L
```
✅ سيظهر badge "خصم 20%" على المنتج

### مثال 3: منتج بدون كمية متاحة
```excel
name: عصير مانجو
price: 20.00
stock_quantity: 0
image: https://i.imgur.com/mango.jpg
category: عصائر
```
✅ سيظهر المنتج كـ "نفذت الكمية"

---

## 🛠️ API Endpoint للرفع البرمجي

إذا كنت تريد رفع المنتجات برمجياً:

### POST `/api/products/bulk-import`

**Headers**:
```json
{
  "Content-Type": "multipart/form-data",
  "Authorization": "Bearer YOUR_TOKEN"
}
```

**Body** (Form Data):
```
file: [Excel file]
branch_id: 1 (optional)
overwrite: false (optional)
```

**Response**:
```json
{
  "success": true,
  "imported": 150,
  "failed": 2,
  "errors": [
    {
      "row": 45,
      "error": "Invalid price format"
    }
  ]
}
```

---

## 📊 جداول قاعدة البيانات

### جدول `products`:
```sql
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    name_en VARCHAR(255),
    description TEXT,
    description_en TEXT,
    price DECIMAL(10, 2) NOT NULL,
    old_price DECIMAL(10, 2),
    discount_percentage INTEGER DEFAULT 0,
    image TEXT NOT NULL,
    category VARCHAR(100) NOT NULL,
    weight VARCHAR(50),
    barcode VARCHAR(100),
    sku VARCHAR(100),
    brand_id INTEGER,
    stock_quantity INTEGER DEFAULT 0,
    nutrition_info JSONB,
    ingredients TEXT,
    allergens TEXT,
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

### جدول `branch_products`:
```sql
CREATE TABLE branch_products (
    id SERIAL PRIMARY KEY,
    branch_id INTEGER NOT NULL REFERENCES branches(id),
    product_id INTEGER NOT NULL REFERENCES products(id),
    price DECIMAL(10, 2),
    stock_quantity INTEGER DEFAULT 0,
    reserved_quantity INTEGER DEFAULT 0,
    min_stock_level INTEGER DEFAULT 10,
    is_available BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(branch_id, product_id)
);
```

---

## 🔐 الأمان والصلاحيات

- **رفع الملفات**: يتطلب حساب Admin فقط
- **حجم الملف الأقصى**: 10 MB
- **عدد المنتجات الأقصى**: 1000 منتج في ملف واحد
- **التحقق من البيانات**: يتم التحقق من كل صف قبل الإضافة

---

## 📞 الدعم الفني

إذا واجهت مشكلة:
1. تحقق من صيغة الملف (Excel أو CSV)
2. تأكد من وجود الأعمدة الإلزامية
3. راجع الأمثلة في هذا الدليل
4. تواصل مع الدعم الفني

---

## ✅ Checklist قبل الرفع

- [ ] الملف بصيغة `.xlsx` أو `.csv`
- [ ] عمود `name` موجود ومملوء
- [ ] عمود `price` موجود وأرقام صحيحة
- [ ] عمود `image` موجود وروابط صحيحة
- [ ] عمود `category` موجود ومتطابق مع الأقسام الموجودة
- [ ] الصور مرفوعة على منصة خارجية (Imgur مثلاً)
- [ ] راجعت البيانات وتأكدت منها
- [ ] عملت نسخة احتياطية من البيانات الحالية

---

**آخر تحديث**: ديسمبر 2024  
**الإصدار**: 1.0.0
