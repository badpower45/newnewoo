# وثيقة متطلبات المنتج (PRD)
# Lumina Fresh Market - Aloosh Market
**منصة التجارة الإلكترونية للبقالة والمواد الغذائية الطازجة**

---

## 📋 جدول المحتويات

1. [نظرة عامة على المشروع](#نظرة-عامة-على-المشروع)
2. [ملخص تنفيذي](#ملخص-تنفيذي)
3. [رؤية المنتج وأهدافه](#رؤية-المنتج-وأهدافه)
4. [الجمهور المستهدف](#الجمهور-المستهدف)
5. [البنية التقنية](#البنية-التقنية)
6. [المتطلبات الوظيفية](#المتطلبات-الوظيفية)
7. [المتطلبات غير الوظيفية](#المتطلبات-غير-الوظيفية)
8. [نموذج البيانات وقاعدة البيانات](#نموذج-البيانات-وقاعدة-البيانات)
9. [واجهات برمجة التطبيقات (APIs)](#واجهات-برمجة-التطبيقات-apis)
10. [تجربة المستخدم والواجهات](#تجربة-المستخدم-والواجهات)
11. [الأمان والمصادقة](#الأمان-والمصادقة)
12. [خطة النشر والبيئات](#خطة-النشر-والبيئات)
13. [المخاطر والتحديات](#المخاطر-والتحديات)
14. [خارطة الطريق والتطوير المستقبلي](#خارطة-الطريق-والتطوير-المستقبلي)
15. [مؤشرات الأداء الرئيسية (KPIs)](#مؤشرات-الأداء-الرئيسية-kpis)

---

## نظرة عامة على المشروع

### معلومات أساسية

| المعلومة | القيمة |
|---------|--------|
| **اسم المشروع** | Lumina Fresh Market (Aloosh Market) |
| **النوع** | منصة تجارة إلكترونية متكاملة للبقالة |
| **النطاق الجغرافي** | السوق المصري (قابل للتوسع) |
| **اللغة الرئيسية** | العربية (RTL - Right to Left) |
| **الإصدار الحالي** | v0.0.0 (قيد التطوير النشط) |
| **تاريخ البدء** | 2025 |
| **الحالة** | Development - Active |

---

## ملخص تنفيذي

### الفكرة الأساسية

**Lumina Fresh Market** هي منصة تجارة إلكترونية حديثة متخصصة في بيع المواد الغذائية الطازجة والبقالة عبر الإنترنت. المنصة تستهدف السوق المصري بواجهة عربية كاملة (RTL) وتقدم تجربة تسوق سلسة تجمع بين:

- **التكنولوجيا الحديثة**: React 19 + TypeScript + Vite لأداء فائق
- **الدعم الفوري**: نظام دردشة حية بتقنية WebSocket
- **إدارة متعددة الفروع**: دعم عدة مواقع بمخزون وأسعار مختلفة
- **تجربة محلية أصيلة**: محتوى عربي 100% مع أسماء منتجات ووصفات مصرية شعبية

### القيمة المضافة

1. **سهولة الوصول**: طلب احتياجاتك اليومية من البيت دون الحاجة للذهاب للسوبر ماركت
2. **الجودة والطازجة**: ضمان منتجات طازجة مع نظام تتبع الجودة
3. **الأسعار التنافسية**: عروض أسبوعية وخصومات خاصة (رمضان، أعياد، مناسبات)
4. **الدعم الفوري**: فريق خدمة عملاء متاح عبر الدردشة المباشرة
5. **برنامج الولاء**: نقاط مكافآت لكل عملية شراء

---

## رؤية المنتج وأهدافه

### الرؤية (Vision)

> "أن نصبح المنصة الرقمية الأولى للتسوق الغذائي في مصر والشرق الأوسط، نوفر لعملائنا تجربة تسوق استثنائية تجمع بين السرعة والجودة والأسعار المناسبة، مع الحفاظ على الطابع المحلي الأصيل"

### الأهداف الاستراتيجية

#### أهداف قصيرة المدى (3-6 أشهر)
1. ✅ إطلاق النسخة التجريبية (MVP) للمنصة
2. ✅ تفعيل نظام الطلبات والدفع الإلكتروني
3. 🎯 الوصول لـ 1,000 عميل مسجل
4. 🎯 تشغيل 3 فروع تجريبية في القاهرة الكبرى
5. 🎯 تحقيق متوسط 50 طلب يومياً

#### أهداف متوسطة المدى (6-12 شهر)
1. التوسع لـ 10 فروع في محافظات مختلفة
2. إطلاق تطبيق الموبايل (iOS & Android)
3. إضافة خدمة التوصيل السريع (Same-day delivery)
4. تفعيل نظام الاشتراكات الشهرية
5. الوصول لـ 10,000 عميل نشط

#### أهداف طويلة المدى (1-3 سنوات)
1. التوسع الإقليمي (دول الخليج، الشام، شمال أفريقيا)
2. إطلاق علامة تجارية خاصة (Private Label)
3. دمج تقنية الذكاء الاصطناعي للتوصيات الشخصية
4. برنامج شراكات مع المزارعين المحليين (Farm to Table)

---

## الجمهور المستهدف

### شرائح العملاء الرئيسية

#### 1. الأسر المصرية (Primary Persona)
- **الخصائص**:
  - عائلات من 3-6 أفراد
  - الشريحة العمرية: 25-45 سنة
  - الدخل: متوسط إلى فوق المتوسط
- **الاحتياجات**:
  - توفير الوقت في التسوق
  - ضمان جودة المنتجات الطازجة
  - الحصول على عروض وخصومات
- **السلوك**:
  - يفضلون الشراء الأسبوعي بكميات كبيرة
  - يبحثون عن المنتجات المصرية الأصلية
  - حساسون للأسعار ويقارنون العروض

#### 2. الشباب والمحترفون (Secondary Persona)
- **الخصائص**:
  - فرديون أو متزوجون حديثاً
  - الشريحة العمرية: 22-35 سنة
  - التكنولوجيا: مستخدمون نشطون للتطبيقات
- **الاحتياجات**:
  - سرعة في الطلب والتوصيل
  - واجهة مستخدم بسيطة وسريعة
  - خيارات دفع متنوعة
- **السلوك**:
  - طلبات متكررة بكميات صغيرة
  - نشطون على وسائل التواصل الاجتماعي
  - يفضلون المنتجات الجاهزة والسناكس

#### 3. أصحاب الأعمال الصغيرة
- **الخصائص**:
  - أصحاب مطاعم، كافيهات، بقالات صغيرة
  - يبحثون عن أسعار الجملة
- **الاحتياجات**:
  - طلبات بالجملة
  - فواتير رسمية
  - جدولة التوصيل

---

## البنية التقنية

### معمارية النظام (System Architecture)

```
┌─────────────────────────────────────────────────────────┐
│                    Frontend Layer                        │
│  ┌──────────────────────────────────────────────────┐   │
│  │  React 19 + TypeScript + Vite                    │   │
│  │  • Pages: Home, Products, Cart, Checkout, Admin  │   │
│  │  • Components: Reusable UI elements              │   │
│  │  • Context: Auth, Cart, Favorites state          │   │
│  │  • Services: API calls, Socket.io client         │   │
│  └──────────────────────────────────────────────────┘   │
│                         ↕ HTTP/WS                        │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Styling & UI Libraries                          │   │
│  │  • Tailwind CSS (Utility-first)                  │   │
│  │  • Framer Motion (Animations)                    │   │
│  │  • Lucide React (Icons)                          │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                           ↕
┌─────────────────────────────────────────────────────────┐
│                    Backend Layer                         │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Node.js + Express 4.19.2                        │   │
│  │  • REST API Endpoints                            │   │
│  │  • WebSocket Server (Socket.io)                  │   │
│  │  • JWT Authentication Middleware                 │   │
│  └──────────────────────────────────────────────────┘   │
│                         ↕ SQL                            │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Database Layer                                   │   │
│  │  • PostgreSQL 8.16 (Production - Supabase)       │   │
│  │  • SQLite 3 (Local Development)                  │   │
│  │  • Connection Pooling                            │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                           ↕
┌─────────────────────────────────────────────────────────┐
│                External Services Layer                   │
│  • Netlify (Frontend Hosting)                           │
│  • Supabase (PostgreSQL Database as a Service)          │
│  • Socket.io (Real-time communication)                  │
└─────────────────────────────────────────────────────────┘
```

### التقنيات المستخدمة (Technology Stack)

#### Frontend Stack
```json
{
  "framework": "React 19.2.0",
  "language": "TypeScript 5.8.2",
  "buildTool": "Vite 6.2.0",
  "routing": "React Router DOM 7.9.6",
  "stateManagement": "React Context API",
  "styling": "Tailwind CSS 3.x",
  "httpClient": "Axios 1.13.2",
  "realTime": "Socket.io Client 4.8.1",
  "animations": "Framer Motion 12.23.24",
  "icons": "Lucide React 0.554.0",
  "barcode": "html5-qrcode 2.3.8",
  "excel": "XLSX 0.18.5"
}
```

#### Backend Stack
```json
{
  "runtime": "Node.js (ES Modules)",
  "framework": "Express 4.19.2",
  "database": {
    "production": "PostgreSQL 8.16.3 (Supabase)",
    "development": "SQLite 3.1.7"
  },
  "authentication": "JWT (jsonwebtoken 9.0.2)",
  "passwordHashing": "bcryptjs 2.4.3",
  "realTime": "Socket.io 4.8.1",
  "cors": "cors 2.8.5",
  "envVars": "dotenv 16.4.5"
}
```

### البيئات التشغيلية (Environments)

| البيئة | الاستخدام | Frontend Port | Backend Port | Database |
|--------|-----------|---------------|--------------|----------|
| **Development** | التطوير المحلي | 5173 (Vite) | 3001 | SQLite |
| **Staging** | الاختبار قبل النشر | Netlify Preview | 3001 | PostgreSQL (Supabase) |
| **Production** | البيئة الحية | Netlify | 3001 | PostgreSQL (Supabase) |

---

## المتطلبات الوظيفية

### 1. نظام إدارة المستخدمين (User Management)

#### 1.1 التسجيل والمصادقة

**المتطلبات:**
- ✅ تسجيل حساب جديد بالبريد الإلكتروني وكلمة المرور
- ✅ تسجيل الدخول باستخدام JWT Tokens
- ✅ تخزين حالة المصادقة في Context API
- ✅ الحفاظ على الجلسة في localStorage
- 🔄 استعادة كلمة المرور (Planned)
- 🔄 تسجيل الدخول عبر Google/Facebook (Planned)

**حقول التسجيل:**
```typescript
interface User {
  id: number;
  name: string;
  email: string; // UNIQUE
  password: string; // Hashed with bcrypt
  role: 'customer' | 'employee' | 'manager' | 'admin';
  default_branch_id?: number;
  loyalty_points: number; // Default: 0
}
```

**سير العمل (Workflow):**
1. المستخدم يدخل البيانات في نموذج التسجيل
2. الـ Frontend يرسل POST request إلى `/api/auth/register`
3. Backend يتحقق من عدم وجود البريد الإلكتروني
4. يتم تشفير كلمة المرور باستخدام bcryptjs
5. يتم إنشاء السجل في قاعدة البيانات
6. يتم إصدار JWT Token (صلاحية 24 ساعة)
7. Frontend يخزن Token في localStorage ويحدث AuthContext

#### 1.2 الأدوار والصلاحيات (Roles & Permissions)

| الدور | الصلاحيات |
|-------|-----------|
| **Customer** | التصفح، الشراء، عرض الطلبات، الدردشة مع الدعم |
| **Employee** | عرض المنتجات والطلبات، الرد على الدردشات |
| **Manager** | إدارة المنتجات والطلبات للفرع المحدد، إدارة الموظفين |
| **Admin** | صلاحيات كاملة على النظام، لوحة التحكم الرئيسية |

#### 1.3 الملف الشخصي (User Profile)

**الصفحات المطلوبة:**
- ✅ عرض بيانات المستخدم
- ✅ تعديل الاسم والبريد الإلكتروني
- ✅ عرض نقاط الولاء
- ✅ سجل الطلبات السابقة
- 🔄 إدارة العناوين المحفوظة
- 🔄 تغيير كلمة المرور

---

### 2. نظام إدارة المنتجات (Product Management)

#### 2.1 عرض المنتجات (Product Display)

**الصفحات:**
- ✅ الصفحة الرئيسية: عرض منتجات مميزة (New Arrivals, Best Sellers)
- ✅ صفحة المنتجات: عرض جميع المنتجات مع الفلترة والبحث
- ✅ صفحة تفاصيل المنتج: معلومات كاملة + تقييمات + صور

**بنية المنتج:**
```typescript
interface Product {
  id: string; // TEXT (e.g., '1', '2', 'uuid')
  name: string;
  description?: string;
  category: string; // 'ياميش', 'خضروات', 'مخبوزات', إلخ
  rating: number; // 0-5
  reviews: number;
  image: string; // URL
  is_organic: boolean;
  weight: string; // '1 كجم', '500 جرام'
  is_new: boolean;
  barcode?: string; // UNIQUE
}

interface BranchProduct {
  branch_id: number;
  product_id: string;
  price: number;
  discount_price?: number;
  stock_quantity: number;
  is_available: boolean;
}
```

#### 2.2 البحث والفلترة

**معايير البحث:**
- ✅ البحث بالاسم (Search by name)
- ✅ الفلترة بالفئة (Category filter)
- 🔄 الفلترة بالسعر (Price range)
- 🔄 الترتيب (السعر، التقييم، الأحدث)
- ✅ البحث بالباركود (Barcode scanner)

**الفئات الرئيسية:**
```typescript
const CATEGORIES = [
  'خضار وفواكه',
  'لحوم ودواجن',
  'أسماك ومأكولات بحرية',
  'تمور ومكسرات',
  'عيش طازة',
  'جبن وألبان',
  'رز ومكرونة',
  'زيت وسمنة',
  'شيبسي ومقرمشات',
  'مشروبات غازية',
  'شوكولاتة وحلويات'
];
```

#### 2.3 مسح الباركود (Barcode Scanning)

**الميزة:**
- ✅ استخدام كاميرا الهاتف/الكمبيوتر لمسح الباركود
- ✅ البحث عن المنتج في قاعدة البيانات
- ✅ عرض تفاصيل المنتج مباشرة
- ✅ إضافة للسلة بضغطة واحدة

**المكتبة المستخدمة:** `html5-qrcode 2.3.8`

---

### 3. نظام سلة التسوق (Shopping Cart)

#### 3.1 إدارة السلة

**الوظائف:**
- ✅ إضافة منتج للسلة
- ✅ إزالة منتج من السلة
- ✅ تحديث الكمية
- ✅ حساب الإجمالي
- ✅ تطبيق الخصومات
- ✅ عرض الأسعار الأصلية والمخفضة

**التخزين:**
- **للزوار (Guests)**: localStorage (CartContext)
- **للمستخدمين المسجلين**: PostgreSQL database (cart table)

**بنية السلة:**
```typescript
interface CartItem {
  id: number;
  user_id: number;
  product_id: string;
  quantity: number;
}
```

#### 3.2 المفضلة (Favorites/Wishlist)

**الوظائف:**
- ✅ إضافة/إزالة من المفضلة
- ✅ عرض قائمة المفضلة
- ✅ نقل من المفضلة للسلة

**التخزين:** FavoritesContext + localStorage

---

### 4. نظام الطلبات (Orders Management)

#### 4.1 إنشاء الطلب (Order Creation)

**خطوات الطلب:**
1. عرض ملخص السلة
2. اختيار الفرع للتوصيل
3. إدخال بيانات التوصيل (عنوان، هاتف)
4. اختيار طريقة الدفع
5. تأكيد الطلب

**بنية الطلب:**
```typescript
interface Order {
  id: number;
  user_id: number;
  branch_id: number; // الفرع المسؤول عن التوصيل
  total: number; // السعر الإجمالي
  items: OrderItem[]; // JSONB في PostgreSQL
  date: Date;
  status: 'pending' | 'confirmed' | 'preparing' | 'out_for_delivery' | 'delivered' | 'cancelled';
}

interface OrderItem {
  product_id: string;
  name: string;
  price: number;
  quantity: number;
  image: string;
}
```

#### 4.2 تتبع الطلب (Order Tracking)

**الحالات:**
- ⏳ **Pending**: بانتظار التأكيد
- ✅ **Confirmed**: تم تأكيد الطلب
- 🔥 **Preparing**: جاري التحضير
- 🚚 **Out for Delivery**: في الطريق
- 🎉 **Delivered**: تم التوصيل
- ❌ **Cancelled**: ملغي

**الإشعارات:**
- 🔄 إشعار عند تغيير حالة الطلب (Planned)
- 🔄 تتبع الموقع الحي للطلب (Planned)

---

### 5. نظام الدعم الفني (Customer Support)

#### 5.1 الدردشة المباشرة (Live Chat)

**الميزات:**
- ✅ نافذة دردشة عائمة (Chat Widget)
- ✅ بدء محادثة جديدة
- ✅ عرض المحادثات السابقة
- ✅ إرسال واستقبال رسائل في الوقت الفعلي (WebSocket)
- ✅ مؤشر الكتابة (Typing indicator)
- ✅ تخصيص محادثة لموظف معين

**التقنية:** Socket.io (Real-time bidirectional communication)

**بنية البيانات:**
```typescript
interface Conversation {
  id: number;
  customer_id: number;
  customer_name: string;
  agent_id?: number; // الموظف المسؤول
  status: 'active' | 'closed' | 'pending';
  created_at: Date;
  last_message_at: Date;
}

interface Message {
  id: number;
  conversation_id: number;
  sender_id: number;
  sender_type: 'customer' | 'agent';
  message: string;
  timestamp: Date;
  is_read: boolean;
}
```

**Socket Events:**
```typescript
// Client -> Server
socket.emit('send_message', { conversationId, message, senderId });
socket.emit('typing', { conversationId });

// Server -> Client
socket.on('receive_message', (message) => {...});
socket.on('user_typing', (data) => {...});
socket.on('conversation_assigned', (data) => {...});
```

#### 5.2 لوحة تحكم الدعم (Support Dashboard)

**للموظفين:**
- ✅ عرض جميع المحادثات
- ✅ تصفية حسب الحالة (نشطة، معلقة، مغلقة)
- ✅ تخصيص محادثة لموظف
- ✅ عرض تاريخ المحادثات
- ✅ إغلاق محادثة

---

### 6. لوحة التحكم الإدارية (Admin Dashboard)

#### 6.1 نظرة عامة (Overview/Analytics)

**المقاييس المطلوبة:**
- 📊 إجمالي الطلبات اليوم/الأسبوع/الشهر
- 💰 إجمالي المبيعات
- 👥 عدد العملاء الجدد
- 📦 المنتجات الأكثر مبيعاً
- 🏪 أداء الفروع
- 📈 رسوم بيانية للمبيعات (Planned)

#### 6.2 إدارة المنتجات (Product Management)

**الوظائف:**
- ✅ عرض جميع المنتجات
- ✅ إضافة منتج جديد
- ✅ تعديل منتج موجود
- ✅ حذف منتج
- ✅ رفع منتجات بالجملة عبر Excel (XLSX)
- ✅ إدارة المخزون لكل فرع
- ✅ تعيين الأسعار والخصومات حسب الفرع

**رفع ملف Excel:**
```typescript
// المكتبة المستخدمة: xlsx
import * as XLSX from 'xlsx';

// الأعمدة المطلوبة:
// id | name | category | price | stock | image | barcode | weight
```

#### 6.3 إدارة الطلبات (Order Management)

**الوظائف:**
- ✅ عرض جميع الطلبات
- ✅ فلترة حسب الحالة، التاريخ، الفرع
- ✅ تحديث حالة الطلب
- ✅ عرض تفاصيل الطلب
- 🔄 طباعة فاتورة
- 🔄 إلغاء الطلب

#### 6.4 إدارة الموظفين (Employee Management)

**الوظائف:**
- ✅ إضافة موظف جديد
- ✅ تعيين الصلاحيات (role)
- ✅ تخصيص الفرع الافتراضي
- ✅ عرض قائمة الموظفين
- 🔄 تتبع أداء الموظفين

---

### 7. نظام الفروع المتعددة (Multi-Branch System)

#### 7.1 إدارة الفروع

**بنية الفرع:**
```typescript
interface Branch {
  id: number;
  name: string; // مثال: "فرع المعادي", "فرع مدينة نصر"
  location_lat: number; // GPS Latitude
  location_lng: number; // GPS Longitude
  coverage_radius_km: number; // نطاق التوصيل (5 كم افتراضياً)
  is_active: boolean;
}
```

**الميزات:**
- ✅ كل فرع له مخزون منفصل
- ✅ أسعار وخصومات مختلفة لكل فرع
- ✅ تحديد نطاق التوصيل بناءً على المسافة
- 🔄 اختيار الفرع الأقرب تلقائياً (Geolocation)

---

### 8. المتطلبات التشغيلية الخاصة بالبقالة (Grocery Specifics)

#### 8.1 إدارة المخزون والبدائل (Stock & Substitution Management)

**سيناريو نفاذ الكمية:**
عندما يطلب العميل منتج ويكتشف الموظف أثناء تحضير الطلب أن المنتج غير متوفر:

**حلول مقترحة:**
- ✅ **تفضيلات الاستبدال**: السماح للعميل بتحديد خيار لكل منتج في السلة:
  ```typescript
  interface CartItemPreferences {
    substitution_preference:
      | 'call_me'              // اتصل بي أولاً
      | 'substitute_similar'   // استبدل بمنتج مشابه
      | 'cancel_item'          // الغِ هذا المنتج فقط
      | 'cancel_order';        // الغِ الطلب كله
  }
  ```

**المنتجات الموزونة (Variable Weight Items):**
منتجات مثل اللحوم، الجبن، الخضروات تباع بالوزن الفعلي:

**التحديات:**
- العميل يطلب "1 كجم دجاج" لكن الوزن الفعلي قد يكون 1.05 كجم أو 0.95 كجم
- السعر النهائي يختلف عن السعر المعروض في السلة

**الحلول:**
```typescript
interface WeightedProduct {
  is_weighted: boolean;
  ordered_weight: number;      // ما طلبه العميل: 1.0 كجم
  actual_weight?: number;      // الوزن الفعلي: 1.05 كجم
  estimated_price: number;     // السعر التقديري
  final_price?: number;        // السعر النهائي بعد الوزن
}
```

**خيارات التنفيذ:**
1. **Pre-authorization**: حجز مبلغ إضافي 10% على البطاقة (مثلاً: طلب بـ 100 ج.م يحجز 110 ج.م)
2. **Adjust on delivery**: تعديل الفاتورة النهائية عند التوصيل
3. **Cash only for weighted**: المنتجات الموزونة تتطلب دفع عند الاستلام

**UI Requirements:**
- عرض "سعر تقديري" بوضوح في صفحة المنتج
- توضيح "قد يختلف السعر النهائي بنسبة ±5%"
- إرسال إشعار للعميل عند تحديد الوزن النهائي

#### 8.2 جدولة التوصيل (Delivery Time Slots)

**المشكلة:**
لا يكفي أن نقول "سيصل الطلب اليوم"، العميل يحتاج تحديد موعد دقيق يناسبه.

**الحل: نظام Slots**

```typescript
interface DeliverySlot {
  id: number;
  branch_id: number;
  date: Date;               // 2025-12-01
  start_time: string;       // "18:00"
  end_time: string;         // "20:00"
  max_orders: number;       // حد أقصى 20 طلب في هذا الوقت
  current_orders: number;   // عدد الطلبات الحالية
  is_available: boolean;    // هل متاح للحجز
  delivery_fee: number;     // رسوم التوصيل (قد تختلف حسب الوقت)
}
```

**مثال على Slots:**
```
اليوم (الأحد 1 ديسمبر):
  ☑️ 10:00 صباحاً - 12:00 ظهراً (متاح - 12/20 طلب)
  ☑️ 12:00 ظهراً - 2:00 مساءً (متاح - 8/20 طلب)
  ☑️ 2:00 مساءً - 4:00 مساءً (متاح - 15/20 طلب)
  ❌ 4:00 مساءً - 6:00 مساءً (ممتلئ - 20/20 طلب)
  ☑️ 6:00 مساءً - 8:00 مساءً (متاح - 3/20 طلب)
  ☑️ 8:00 مساءً - 10:00 مساءً (متاح - 0/20 طلب)

غداً (الإثنين 2 ديسمبر):
  ☑️ 9:00 صباحاً - 11:00 صباحاً...
```

**Database Schema Addition:**
```sql
CREATE TABLE delivery_slots (
  id SERIAL PRIMARY KEY,
  branch_id INTEGER REFERENCES branches(id),
  date DATE NOT NULL,
  start_time TIME NOT NULL,
  end_time TIME NOT NULL,
  max_orders INTEGER DEFAULT 20,
  delivery_fee DECIMAL(10,2) DEFAULT 15.00,
  is_active BOOLEAN DEFAULT TRUE,
  UNIQUE(branch_id, date, start_time)
);

-- ربط الطلب بالـ Slot
ALTER TABLE orders
ADD COLUMN delivery_slot_id INTEGER REFERENCES delivery_slots(id);
```

**Capacity Planning:**
- إنشاء Slots تلقائياً لـ 7 أيام قادمة
- منع حجز Slot إذا وصل `current_orders >= max_orders`
- تحديث `current_orders` عند إنشاء/إلغاء طلب

#### 8.3 إدارة الصور (Image Hosting & Management)

**المشكلة الحالية:**
- استخدام روابط خارجية (Unsplash) للمنتجات
- ملف `multer` موجود لكن التخزين محلي (سيضيع عند Restart على Netlify/Serverless)

**الحل المقترح:**

**خيارات التخزين السحابي:**

1. **Cloudinary** (مجاني حتى 25 GB):
```typescript
import { v2 as cloudinary } from 'cloudinary';

cloudinary.config({
  cloud_name: process.env.CLOUDINARY_CLOUD_NAME,
  api_key: process.env.CLOUDINARY_API_KEY,
  api_secret: process.env.CLOUDINARY_API_SECRET
});

// رفع صورة
const result = await cloudinary.uploader.upload(file.path, {
  folder: 'lumina-products',
  transformation: [
    { width: 800, height: 800, crop: 'limit' },
    { quality: 'auto' }
  ]
});

product.image = result.secure_url; // حفظ الرابط
```

2. **Supabase Storage** (مدمج مع قاعدة البيانات):
```typescript
import { createClient } from '@supabase/supabase-js';

const supabase = createClient(SUPABASE_URL, SUPABASE_KEY);

const { data, error } = await supabase.storage
  .from('product-images')
  .upload(`products/${productId}.jpg`, file);

// URL: https://xxx.supabase.co/storage/v1/object/public/product-images/products/123.jpg
```

**التوصية:**
- استخدم **Cloudinary** للـ MVP (سهل ومجاني)
- الانتقال لـ Supabase Storage لاحقاً للتكامل الكامل

**تحديث API:**
```typescript
// POST /api/products/upload-image
router.post('/upload-image',
  upload.single('image'), // Multer middleware
  async (req, res) => {
    const result = await cloudinary.uploader.upload(req.file.path);
    res.json({ imageUrl: result.secure_url });
  }
);
```

#### 8.4 دورة حياة الطلب وإدارة المخزون (Order Lifecycle & Inventory)

**المشكلة:**
متى يتم خصم الكمية من المخزون؟ إذا خصمناها عند إضافة المنتج للسلة، قد يحجز العملاء منتجات ولا يشترونها (Cart Abandonment). وإذا لم نخصمها، قد نبيع منتج غير موجود (Overselling).

**الحل: State Machine للطلب**

```
[Cart] → [Order Created] → [Payment Pending] → [Confirmed] → [Preparing] → [Out for Delivery] → [Delivered]
          ↓ (خصم مخزون)    ↓ (فشل الدفع)   ↓              ↓
        Reserve Stock     Restore Stock   Deduct Final   Update Inventory
```

**Implementation:**
```typescript
// عند إنشاء الطلب
async function createOrder(userId, items) {
  const transaction = await db.transaction();

  try {
    // 1. إنشاء Order
    const order = await Order.create({ userId, status: 'pending' });

    // 2. حجز المخزون (Soft Reserve)
    for (const item of items) {
      await db.query(`
        UPDATE branch_products
        SET reserved_quantity = reserved_quantity + $1
        WHERE product_id = $2 AND branch_id = $3
        AND (stock_quantity - reserved_quantity) >= $1
      `, [item.quantity, item.productId, branchId]);

      // إذا فشل (مخزون غير كافٍ), rollback
      if (result.rowCount === 0) {
        throw new Error(`Product ${item.name} out of stock`);
      }
    }

    await transaction.commit();
    return order;
  } catch (error) {
    await transaction.rollback();
    throw error;
  }
}

// عند تأكيد الدفع
async function confirmPayment(orderId) {
  await db.query(`
    UPDATE branch_products bp
    SET
      stock_quantity = stock_quantity - oi.quantity,
      reserved_quantity = reserved_quantity - oi.quantity
    FROM order_items oi
    WHERE oi.order_id = $1 AND bp.product_id = oi.product_id
  `, [orderId]);

  await Order.update({ status: 'confirmed' }, { where: { id: orderId }});
}
```

**Schema Update:**
```sql
ALTER TABLE branch_products
ADD COLUMN reserved_quantity INTEGER DEFAULT 0;

-- الكمية المتاحة فعلياً = stock_quantity - reserved_quantity
```

#### 8.5 طرق الدفع (في السياق المصري)

**الطرق الأكثر استخداماً في مصر:**

| الطريقة | الشعبية | الرسوم | التكامل |
|---------|---------|--------|---------|
| **الدفع عند الاستلام (COD)** | 🔥🔥🔥🔥🔥 | 0% | مباشر |
| **المحافظ الإلكترونية** | 🔥🔥🔥🔥 | 1-2% | Fawry, Vodafone Cash |
| **InstaPay** | 🔥🔥🔥 | 0% | QR Code |
| **Visa/Mastercard** | 🔥🔥 | 2.5-3% | Paymob, Fawry |

**التوصية للـ MVP:**
1. **الدفع عند الاستلام (COD)** - أولوية 1 ✅
2. **Fawry** (يدعم: محافظ + بطاقات + أكشاك) - أولوية 2
3. **Paymob** (للبطاقات الائتمانية) - أولوية 3

**Payment Flow:**
```typescript
interface Order {
  // ...
  payment_method: 'cod' | 'fawry' | 'card' | 'instapay';
  payment_status: 'pending' | 'paid' | 'failed' | 'refunded';
  payment_transaction_id?: string;
}

// عند الـ Checkout
if (paymentMethod === 'cod') {
  order.payment_status = 'pending'; // سيدفع عند الاستلام
} else if (paymentMethod === 'fawry') {
  // Redirect to Fawry payment gateway
  const fawryResponse = await initiateFawryPayment(order);
  // سيتم التحديث عبر Webhook
}
```

---

### 9. العروض والتسويق (Promotions & Marketing)

#### 8.1 صفحة الهبوط (Landing Page)

**العناصر:**
- ✅ **Hero Section**: Bento Grid Layout بعروض رئيسية
  - مهرجان الشوكولاتة والسعادة
  - جبنة رومي وتلاجة
  - مولتو وباتيه
- ✅ **Promo Banners**:
  - كرتونة الخير والبركة (رمضان)
  - منتجات ألبان الصباح
  - قرمشة وسهرة الخميس
- ✅ **Magazine Offers**: مجلات العروض الأسبوعية
  - حلويات العيد
  - تسالي ومقرمشات
  - ياميش ومكسرات
  - خزين البيت

#### 8.2 الأنيميشن والتفاعل

**المؤثرات:**
- ✅ Framer Motion للانتقالات السلسة
- ✅ Falling Snacks Animation (سناكس متساقطة في الخلفية)
- ✅ Hover effects على بطاقات المنتجات
- ✅ Smooth Scrolling

#### 8.3 محتوى فيديو (Reels/Short Videos)

**قسم Reels:**
- ✅ وصفات طبخ ("طريقة عمل الكنافة بالمانجا")
- ✅ ريفيو منتجات ("تحدي الشيبسي الجديد")
- ✅ نصائح توفير ("مشتريات رمضان بـ 500 جنيه")
- ✅ تجارب ("جربنا عصير القصب بالفراولة")

**البيانات:**
```typescript
interface ReelItem {
  id: number;
  title: string;
  views: string; // "1.2M"
  author: string;
  videoImage: string;
  type: 'وصفة' | 'ريفيو' | 'توفير' | 'تجربة' | 'نصيحة';
}
```

---

## المتطلبات غير الوظيفية

### 1. الأداء (Performance)

| المقياس | الهدف | الحالي |
|---------|-------|--------|
| **First Contentful Paint (FCP)** | < 1.5s | ✅ محقق |
| **Time to Interactive (TTI)** | < 3s | 🔄 قيد القياس |
| **Page Load Time** | < 2s | ✅ Vite HMR سريع |
| **API Response Time** | < 500ms | 🔄 يعتمد على Supabase |
| **WebSocket Latency** | < 100ms | ✅ Socket.io فوري |

**التحسينات:**
- ✅ Code Splitting مع React.lazy()
- ✅ Tree Shaking في Vite
- ✅ Image Optimization (Unsplash CDN)
- 🔄 Lazy Loading للصور
- 🔄 Server-Side Caching

### 2. الأمان (Security)

**الإجراءات المطبقة:**
- ✅ JWT Authentication
- ✅ Password Hashing (bcryptjs)
- ✅ CORS Configuration
- ✅ SQL Injection Protection (Parameterized Queries)
- ⚠️ **تحذير أمني**: ملف .env يحتوي على credentials مكشوفة في الكود

**التحسينات المطلوبة:**
- 🔴 **عاجل**: حذف .env من Git وإضافته لـ .gitignore
- 🔴 تدوير Database Credentials
- 🔄 HTTPS Enforcement
- 🔄 Rate Limiting على APIs
- 🔄 Input Validation & Sanitization
- 🔄 XSS Protection
- 🔄 CSRF Tokens

### 3. قابلية التوسع (Scalability)

**الاستراتيجية:**
- ✅ Database Connection Pooling (PostgreSQL)
- ✅ Stateless Backend (يمكن توسيعه أفقياً)
- 🔄 CDN للصور والملفات الثابتة
- 🔄 Redis لـ Caching
- 🔄 Load Balancing للـ Backend
- 🔄 Database Replication

**الطاقة الاستيعابية المستهدفة:**
- 1,000 مستخدم متزامن
- 10,000 طلب يومياً
- 100,000 منتج في الكتالوج

### 4. التوافقية (Compatibility)

#### المتصفحات المدعومة:
- ✅ Chrome/Edge (Latest 2 versions)
- ✅ Firefox (Latest 2 versions)
- ✅ Safari (Latest 2 versions)
- ✅ Mobile Browsers (iOS Safari, Chrome Mobile)

#### الأجهزة:
- ✅ Desktop (1920x1080 and above)
- ✅ Laptop (1366x768 and above)
- ✅ Tablet (768x1024)
- ✅ Mobile (375x667 and above)

#### الدعم اللغوي:
- ✅ العربية (RTL) - اللغة الأساسية
- 🔄 الإنجليزية (LTR) - مخطط لها

### 5. إمكانية الوصول (Accessibility)

**معايير WCAG 2.1:**
- 🔄 Level A (أساسي)
- 🔄 Level AA (مستهدف)
- ⏸️ Level AAA (مستقبلي)

**الميزات:**
- ✅ RTL Support (دعم الكتابة من اليمين لليسار)
- ✅ خطوط عربية واضحة (Cairo, Tajawal)
- 🔄 Keyboard Navigation
- 🔄 Screen Reader Support
- 🔄 Alt Text للصور
- 🔄 Contrast Ratios (4.5:1)

### 6. الصيانة (Maintainability)

**أفضل الممارسات:**
- ✅ TypeScript لتقليل الأخطاء
- ✅ Component-Based Architecture (React)
- ✅ Consistent File Structure
- 🔄 ESLint & Prettier
- 🔄 Unit Tests (Jest/Vitest)
- 🔄 Documentation (JSDoc)

---

## نموذج البيانات وقاعدة البيانات

### مخطط قاعدة البيانات (Database Schema)

```sql
┌─────────────────────────────────────────────────────────┐
│                     Database: PostgreSQL                 │
└─────────────────────────────────────────────────────────┘

┌──────────────────────┐         ┌──────────────────────┐
│       users          │         │      branches        │
├──────────────────────┤         ├──────────────────────┤
│ id (SERIAL PK)       │         │ id (SERIAL PK)       │
│ name (TEXT)          │         │ name (TEXT)          │
│ email (TEXT UNIQUE)  │         │ location_lat (DEC)   │
│ password (TEXT)      │         │ location_lng (DEC)   │
│ role (TEXT)          │◄────┐   │ coverage_radius_km   │
│ default_branch_id    │     │   │ is_active (BOOL)     │
│ loyalty_points (INT) │     │   └──────────────────────┘
└──────────────────────┘     │            │
         │                   │            │
         │                   │            │
         ▼                   │            ▼
┌──────────────────────┐     │   ┌──────────────────────┐
│       cart           │     │   │  branch_products     │
├──────────────────────┤     │   ├──────────────────────┤
│ id (SERIAL PK)       │     │   │ branch_id (FK)       │
│ user_id (FK)         │     │   │ product_id (FK)      │
│ product_id (FK)      │     │   │ price (DECIMAL)      │
│ quantity (INT)       │     │   │ discount_price       │
└──────────────────────┘     │   │ stock_quantity       │
                             │   │ is_available (BOOL)  │
┌──────────────────────┐     │   └──────────────────────┘
│      products        │     │            ▲
├──────────────────────┤     │            │
│ id (TEXT PK)         │─────┼────────────┘
│ name (TEXT)          │     │
│ description (TEXT)   │     │
│ category (TEXT)      │     │
│ rating (DECIMAL)     │     │
│ reviews (INT)        │     │
│ image (TEXT)         │     │
│ is_organic (BOOL)    │     │
│ weight (TEXT)        │     │
│ is_new (BOOL)        │     │
│ barcode (TEXT UNIQUE)│     │
└──────────────────────┘     │
         │                   │
         │                   │
         ▼                   │
┌──────────────────────┐     │
│       orders         │     │
├──────────────────────┤     │
│ id (SERIAL PK)       │     │
│ user_id (FK)         │─────┤
│ branch_id (FK)       │     │
│ total (DECIMAL)      │     │
│ items (JSONB)        │     │
│ date (TIMESTAMP)     │     │
│ status (TEXT)        │     │
└──────────────────────┘     │
                             │
┌──────────────────────┐     │
│   conversations      │     │
├──────────────────────┤     │
│ id (SERIAL PK)       │     │
│ customer_id (FK)     │─────┤
│ customer_name (TEXT) │     │
│ agent_id (FK)        │─────┘
│ status (TEXT)        │
│ created_at (TS)      │
│ last_message_at (TS) │
└──────────────────────┘
         │
         │
         ▼
┌──────────────────────┐
│      messages        │
├──────────────────────┤
│ id (SERIAL PK)       │
│ conversation_id (FK) │
│ sender_id (FK)       │
│ sender_type (TEXT)   │
│ message (TEXT)       │
│ timestamp (TS)       │
│ is_read (BOOL)       │
└──────────────────────┘
```

### الفهارس (Indexes)

```sql
CREATE INDEX idx_branch_products_branch ON branch_products(branch_id);
CREATE INDEX idx_branch_products_product ON branch_products(product_id);
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_products_category ON products(category);
```

**الغرض:**
- تسريع استعلامات البحث عن المنتجات حسب الفرع
- تحسين أداء Login (البحث بالـ email)
- تحسين الفلترة بالفئات

---

## واجهات برمجة التطبيقات (APIs)

### API Base URL
```
Development: http://localhost:3001/api
Production: https://your-backend-domain.com/api
```

### Authentication Endpoints

#### POST `/api/auth/register`
تسجيل حساب جديد

**Request Body:**
```json
{
  "name": "أحمد محمد",
  "email": "ahmed@example.com",
  "password": "securePassword123"
}
```

**Response (200):**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 123,
    "name": "أحمد محمد",
    "email": "ahmed@example.com",
    "role": "customer",
    "loyalty_points": 0
  }
}
```

#### POST `/api/auth/login`
تسجيل الدخول

**Request Body:**
```json
{
  "email": "ahmed@example.com",
  "password": "securePassword123"
}
```

**Response (200):**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": { ... }
}
```

---

### Product Endpoints

#### GET `/api/products`
الحصول على جميع المنتجات

**Query Parameters:**
- `category` (optional): فلترة حسب الفئة
- `search` (optional): البحث بالاسم
- `branch_id` (optional): عرض أسعار فرع معين

**Response (200):**
```json
[
  {
    "id": "1",
    "name": "بلح سيوي فاخر",
    "price": 65.00,
    "originalPrice": 80.00,
    "category": "ياميش",
    "rating": 4.9,
    "reviews": 342,
    "image": "https://...",
    "is_organic": true,
    "weight": "1 كجم",
    "stock_quantity": 50
  }
]
```

#### GET `/api/products/:id`
الحصول على منتج محدد

#### GET `/api/products/barcode/:barcode`
البحث بالباركود

#### POST `/api/products` (Admin Only)
إضافة منتج جديد

**Request Body:**
```json
{
  "name": "منتج جديد",
  "category": "شوكولاتة",
  "description": "وصف المنتج",
  "barcode": "1234567890123",
  "weight": "500 جرام"
}
```

---

### Cart Endpoints

#### GET `/api/cart/:userId`
الحصول على سلة المستخدم

#### POST `/api/cart`
إضافة منتج للسلة

**Request Body:**
```json
{
  "userId": 123,
  "productId": "5",
  "quantity": 2
}
```

#### PUT `/api/cart/:cartItemId`
تحديث كمية منتج في السلة

#### DELETE `/api/cart/:cartItemId`
إزالة منتج من السلة

#### DELETE `/api/cart/user/:userId`
تفريغ السلة كاملة

---

### Order Endpoints

#### POST `/api/orders`
إنشاء طلب جديد

**Request Body:**
```json
{
  "userId": 123,
  "branchId": 1,
  "items": [
    {
      "product_id": "1",
      "name": "بلح سيوي",
      "price": 65,
      "quantity": 2,
      "image": "https://..."
    }
  ],
  "total": 130.00,
  "deliveryAddress": "شارع النصر، مدينة نصر، القاهرة"
}
```

**Response (201):**
```json
{
  "orderId": 456,
  "status": "pending",
  "message": "تم إنشاء الطلب بنجاح"
}
```

#### GET `/api/orders/:userId`
الحصول على طلبات المستخدم

#### GET `/api/orders` (Admin Only)
الحصول على جميع الطلبات

#### PUT `/api/orders/:orderId/status`
تحديث حالة الطلب

---

### Chat Endpoints (REST + WebSocket)

#### POST `/api/chat/conversations`
بدء محادثة جديدة

**Request Body:**
```json
{
  "customerId": 123,
  "customerName": "أحمد محمد"
}
```

#### GET `/api/chat/conversations/:conversationId/messages`
الحصول على رسائل محادثة معينة

#### POST `/api/chat/messages`
إرسال رسالة (بديل عن WebSocket)

---

### WebSocket Events (Socket.io)

#### Client → Server

```typescript
// إرسال رسالة
socket.emit('send_message', {
  conversationId: 10,
  senderId: 123,
  message: 'مرحباً، أريد الاستفسار عن منتج'
});

// مؤشر الكتابة
socket.emit('typing', {
  conversationId: 10,
  senderName: 'أحمد'
});
```

#### Server → Client

```typescript
// استقبال رسالة جديدة
socket.on('receive_message', (message) => {
  console.log(message);
});

// أحدهم يكتب
socket.on('user_typing', (data) => {
  // عرض "أحمد يكتب..."
});

// تم تخصيص محادثة لك
socket.on('conversation_assigned', (conversation) => {
  // تحديث قائمة المحادثات
});
```

---

## تجربة المستخدم والواجهات

### نظام الألوان (Color Palette)

```css
/* ألوان العلامة التجارية */
--brand-brown: #23110C;     /* البني الداكن - الرئيسي */
--brand-orange: #F97316;    /* البرتقالي - التركيز */
--brand-light: #FFF7ED;     /* كريمي فاتح - الخلفيات */

/* ألوان الحالات */
--success: #10B981;         /* أخضر - نجاح */
--warning: #F59E0B;         /* برتقالي - تحذير */
--error: #EF4444;           /* أحمر - خطأ */
--info: #3B82F6;            /* أزرق - معلومات */

/* ألوان الخلفيات */
--bg-primary: #FFFFFF;
--bg-secondary: #F9FAFB;
--bg-dark: #111827;
```

### الخطوط (Typography)

```css
/* الخط الأساسي */
font-family: 'Cairo', 'Tajawal', sans-serif;

/* أحجام الخط */
--text-xs: 0.75rem;    /* 12px */
--text-sm: 0.875rem;   /* 14px */
--text-base: 1rem;     /* 16px */
--text-lg: 1.125rem;   /* 18px */
--text-xl: 1.25rem;    /* 20px */
--text-2xl: 1.5rem;    /* 24px */
--text-3xl: 1.875rem;  /* 30px */
--text-4xl: 2.25rem;   /* 36px */
```

### المكونات الأساسية (Core Components)

#### 1. Header (الهيدر)
- ✅ Logo + Search Bar
- ✅ Mega Menu للأقسام
- ✅ أيقونات: السلة، المفضلة، الحساب
- ✅ عداد عناصر السلة
- ✅ Sticky Header عند السكرول

#### 2. Product Card (بطاقة المنتج)
```tsx
<ProductCard>
  <Image />              {/* صورة المنتج */}
  <Badge>عضوي</Badge>    {/* شارة (إن وجدت) */}
  <Name />               {/* اسم المنتج */}
  <Rating />             {/* التقييم ⭐ */}
  <Price>
    <Original>80 ج.م</Original>
    <Discounted>65 ج.م</Discounted>
  </Price>
  <AddToCartButton />    {/* زر الإضافة */}
  <FavoriteIcon />       {/* القلب للمفضلة */}
</ProductCard>
```

#### 3. Footer (الفوتر)
- روابط: من نحن، اتصل بنا، الشروط والأحكام
- روابط السوشيال ميديا
- طرق الدفع المقبولة
- حقوق النشر

---

### الصفحات الرئيسية (Main Pages)

#### 1. الصفحة الرئيسية (/)
**الأقسام:**
```
┌────────────────────────────────────────┐
│           Header + Mega Menu           │
├────────────────────────────────────────┤
│     Hero Section (Bento Grid)          │
│  ┌─────────┐ ┌────┐ ┌────┐            │
│  │ مهرجان  │ │جبنة│ │مولتو│            │
│  │الشوكولاتة│ │رومي│ │باتيه│            │
│  └─────────┘ └────┘ └────┘            │
├────────────────────────────────────────┤
│     منتجات طازجة (Fresh Products)      │
│  🥬 🍅 🥖 🥩                             │
├────────────────────────────────────────┤
│     Promo Banners (كرتونة الخير...)    │
├────────────────────────────────────────┤
│     خزين البيت (Pantry Products)       │
│  🛢️ 🍚 🍝 ☕                             │
├────────────────────────────────────────┤
│     سناكس (Snacks)                     │
│  🍟 🥤 🍫                                │
├────────────────────────────────────────┤
│     مجلات العروض (Magazine Offers)     │
│  📰 📰 📰                                │
├────────────────────────────────────────┤
│     Reels & Videos                     │
│  🎥 🎥 🎥 🎥                             │
├────────────────────────────────────────┤
│           Footer                       │
└────────────────────────────────────────┘
```

#### 2. صفحة المنتجات (/products)
**الأقسام:**
```
┌────────────────┬─────────────────────────┐
│   Filters      │    Product Grid         │
│                │                         │
│ □ الفئات       │  ┌───┐ ┌───┐ ┌───┐    │
│   □ خضروات     │  │ 1 │ │ 2 │ │ 3 │    │
│   □ فواكه      │  └───┘ └───┘ └───┘    │
│   □ ألبان      │  ┌───┐ ┌───┐ ┌───┐    │
│                │  │ 4 │ │ 5 │ │ 6 │    │
│ السعر          │  └───┘ └───┘ └───┘    │
│ [▬▬▬○────]     │                         │
│                │  Pagination             │
│ التقييم        │  « 1 2 3 »             │
│ ⭐⭐⭐⭐⭐       │                         │
└────────────────┴─────────────────────────┘
```

#### 3. صفحة السلة (/cart)
**المحتوى:**
```
┌──────────────────────────────────────────┐
│  سلة التسوق (3 منتجات)                  │
├──────────────────────────────────────────┤
│  ┌────┐ بلح سيوي فاخر                   │
│  │ 📷 │ 65 ج.م × 2       [-] 2 [+]  [🗑]│
│  └────┘                                  │
├──────────────────────────────────────────┤
│  ┌────┐ زيت كريستال                     │
│  │ 📷 │ 95 ج.م × 1       [-] 1 [+]  [🗑]│
│  └────┘                                  │
├──────────────────────────────────────────┤
│                                          │
│  المجموع الفرعي:        225 ج.م         │
│  رسوم التوصيل:           15 ج.م         │
│  ─────────────────────────────────       │
│  الإجمالي:              240 ج.م         │
│                                          │
│         [ إتمام الطلب ]                  │
└──────────────────────────────────────────┘
```

#### 4. لوحة التحكم الإدارية (/admin)
**الصفحات الفرعية:**
- `/admin/dashboard` - نظرة عامة
- `/admin/products` - إدارة المنتجات
- `/admin/orders` - إدارة الطلبات
- `/admin/employees` - إدارة الموظفين
- `/admin/chat` - لوحة الدردشة

---

### الانيميشن والتفاعل (Animations)

**Framer Motion Variants:**
```typescript
const fadeInUp = {
  initial: { opacity: 0, y: 20 },
  animate: { opacity: 1, y: 0 },
  transition: { duration: 0.5 }
};

const stagger = {
  animate: {
    transition: {
      staggerChildren: 0.1
    }
  }
};
```

**استخدامات:**
- ✅ Fade In للعناصر عند التحميل
- ✅ Slide In للـ Mega Menu
- ✅ Scale على Hover لبطاقات المنتجات
- ✅ Falling Snacks Animation (خلفية متحركة)

---

## الأمان والمصادقة

### 1. المصادقة (Authentication)

**الآلية:** JSON Web Tokens (JWT)

```typescript
// إنشاء Token
const token = jwt.sign(
  {
    userId: user.id,
    email: user.email,
    role: user.role
  },
  process.env.JWT_SECRET, // مفتاح سري
  { expiresIn: '24h' }   // صلاحية 24 ساعة
);
```

**سير العمل:**
1. User يرسل email + password
2. Server يتحقق من صحة البيانات
3. Server ينشئ JWT Token
4. Client يحفظ Token في localStorage
5. كل طلب لاحق يحتوي على Header: `Authorization: Bearer <token>`
6. Middleware يتحقق من صحة Token قبل تنفيذ الطلب

### 2. تأمين كلمات المرور

**المكتبة:** bcryptjs

```javascript
// عند التسجيل
const hashedPassword = await bcrypt.hash(password, 10); // 10 salt rounds

// عند تسجيل الدخول
const isValid = await bcrypt.compare(password, hashedPassword);
```

### 3. الحماية من الثغرات الأمنية

#### SQL Injection
✅ **محمي** - استخدام Parameterized Queries

```javascript
// ❌ خطأ (Vulnerable)
const query = `SELECT * FROM users WHERE email = '${email}'`;

// ✅ صحيح (Safe)
const query = 'SELECT * FROM users WHERE email = $1';
const result = await pool.query(query, [email]);
```

#### XSS (Cross-Site Scripting)
🔄 **جاري التحسين**
- Sanitize user inputs
- Escape HTML in user-generated content
- Content Security Policy headers

#### CSRF (Cross-Site Request Forgery)
🔄 **مخطط**
- CSRF Tokens في النماذج
- SameSite cookie attributes

#### Rate Limiting
🔄 **مخطط**
```javascript
import rateLimit from 'express-rate-limit';

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 دقيقة
  max: 100 // حد أقصى 100 طلب
});

app.use('/api', limiter);
```

### 4. تحذيرات أمنية عاجلة

🔴 **CRITICAL SECURITY ISSUES:**

1. **ملف .env مكشوف في Git**
   ```bash
   # الحل الفوري:
   git rm --cached .env
   echo ".env" >> .gitignore
   git add .gitignore
   git commit -m "Remove .env from version control"
   ```

2. **Database Credentials مكشوفة**
   - URL: `postgresql://postgres.znbxqepwdxbcjdwwuqgo:testtest1234@aws-0-eu-central-1.pooler.supabase.com:6543/postgres`
   - **الإجراء المطلوب:**
     - تدوير كلمة مرور قاعدة البيانات فوراً في Supabase
     - استخدام Environment Variables في الإنتاج
     - عدم رفع credentials أبداً للـ Git

3. **JWT Secret ضعيف**
   ```
   Current: "your-secret-key" ❌
   Required: "W8h#mK9$pL2@nQ5&vB7^xC3!rT6%jF4*" ✅
   ```

---

## خطة النشر والبيئات

### البيئات (Environments)

#### 1. Development (التطوير المحلي)
```bash
# Frontend
npm run dev          # Vite dev server - Port 5173
# URL: http://localhost:5173

# Backend
npm run dev          # Nodemon - Port 3001
# URL: http://localhost:3001

# Database
SQLite local file: ./database.db
```

**الميزات:**
- ✅ Hot Module Replacement (HMR)
- ✅ Source Maps
- ✅ محاكاة WebSocket محلياً

#### 2. Staging (الاختبار)
```bash
# Frontend
Netlify Preview Deployment

# Backend
Node.js server on staging environment

# Database
PostgreSQL - Supabase (staging instance)
```

**الغرض:**
- اختبار الميزات الجديدة قبل النشر
- QA Testing
- User Acceptance Testing (UAT)

#### 3. Production (الإنتاج)
```bash
# Frontend
Netlify - CDN Deployment
# URL: https://lumina-fresh-market.netlify.app

# Backend
VPS/Cloud Server (AWS, DigitalOcean, etc.)
# URL: https://api.lumina-market.com

# Database
PostgreSQL - Supabase Production
```

---

### عملية النشر (Deployment Process)

#### Frontend (Netlify)

**ملف التكوين: netlify.toml**
```toml
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

**خطوات النشر:**
1. Push to main branch
2. Netlify detects changes
3. Runs `npm run build`
4. Deploys `dist/` folder to CDN
5. Atomic deployment (zero downtime)

**Environment Variables في Netlify:**
```
VITE_API_URL=https://api.lumina-market.com
VITE_SOCKET_URL=https://api.lumina-market.com
```

#### Backend (Node.js Server)

**المتطلبات:**
- Node.js 18+
- PostgreSQL connection
- PM2 للـ Process Management

**خطوات النشر:**
```bash
# 1. نقل الملفات للسيرفر
scp -r server/ user@server:/var/www/lumina-backend

# 2. تثبيت Dependencies
cd /var/www/lumina-backend
npm install --production

# 3. تشغيل مع PM2
pm2 start index.js --name lumina-api
pm2 save

# 4. إعداد Nginx Reverse Proxy
# /etc/nginx/sites-available/lumina-api
server {
  listen 80;
  server_name api.lumina-market.com;

  location / {
    proxy_pass http://localhost:3001;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
  }
}
```

#### Database (Supabase)

**الإعداد:**
1. إنشاء مشروع على Supabase
2. تنفيذ `schema.sql`
3. تكوين Connection Pooling
4. إضافة Indexes
5. إعداد Backups تلقائية

**Connection String:**
```
postgresql://[user]:[password]@[host]:6543/postgres
```

---

### مراقبة الأداء (Monitoring)

**أدوات مقترحة:**
- 📊 **Sentry**: تتبع الأخطاء
- 📈 **Google Analytics**: تحليل الزيارات
- ⚡ **Lighthouse**: قياس الأداء
- 🔍 **LogRocket**: تسجيل جلسات المستخدمين
- 💾 **Supabase Dashboard**: مراقبة قاعدة البيانات

**المقاييس المطلوبة:**
- Uptime > 99.5%
- API Response Time < 500ms
- Frontend Load Time < 2s
- Error Rate < 0.1%

---

## المخاطر والتحديات

### المخاطر التقنية

| المخاطر | التأثير | الاحتمالية | الإجراء |
|---------|---------|-----------|---------|
| **Database Credentials Leak** | 🔴 High | Medium | تدوير Passwords فوراً |
| **Supabase Downtime** | 🟡 Medium | Low | إعداد Database Replica |
| **WebSocket Connection Issues** | 🟡 Medium | Medium | Fallback to Polling |
| **Scalability Bottleneck** | 🟡 Medium | High | Load Balancing + Caching |
| **Payment Gateway Integration** | 🟢 Low | High | اختبار شامل قبل الإطلاق |

### التحديات التجارية

1. **المنافسة الشديدة**
   - السوق مزدحم بتطبيقات توصيل البقالة
   - **الحل**: التركيز على تجربة المستخدم والمحتوى المحلي

2. **اكتساب الثقة**
   - المستخدمون حذرون من منصات جديدة
   - **الحل**: برنامج إرجاع مجاني + ضمان الجودة

3. **التوصيل اللوجستي**
   - إدارة الأسطول والتوصيل في الوقت
   - **الحل**: شراكات مع شركات التوصيل

4. **إدارة المخزون**
   - المنتجات الطازجة لها صلاحية قصيرة
   - **الحل**: نظام FIFO + تتبع الصلاحية

---

## خارطة الطريق والتطوير المستقبلي

### المرحلة 1: MVP Launch (شهر 1-2) ✅

- [x] Frontend React + TypeScript
- [x] Backend Express + PostgreSQL
- [x] نظام المصادقة
- [x] عرض وبحث المنتجات
- [x] السلة والطلبات
- [x] الدردشة المباشرة
- [x] لوحة تحكم الإدارة

### المرحلة 2: التحسينات (شهر 3-4) 🔄

- [ ] **تكامل الدفع الإلكتروني**
  - Fawry (أولوية)
  - Paymob
  - InstaPay QR Code
  - Visa/Mastercard
- [ ] **نظام جدولة التوصيل (Delivery Slots)**
  - إنشاء وإدارة Time Slots
  - Capacity Planning
  - اختيار موعد التوصيل في Checkout
- [ ] **واجهة المندوب (Driver Interface)**
  - تطبيق/واجهة ويب للمندوبين
  - عرض الطلبات والعناوين
  - تحديث حالة التوصيل
  - GPS Tracking
- [ ] **إدارة البدائل والمنتجات الموزونة**
  - Substitution Preferences
  - Variable Weight Products
  - تعديل الأسعار النهائية
- [ ] **تحسينات الأمان**
  - Rate Limiting
  - CSRF Protection
  - Security Audit كامل
- [ ] **SEO Optimization**
  - Server-Side Rendering (Next.js migration?)
  - Open Graph Tags
  - Schema.org Markup
- [ ] **Image Hosting**
  - تكامل Cloudinary أو Supabase Storage
  - Image Optimization & Resizing

### المرحلة 3: التوسع (شهر 5-6)

- [ ] **تطبيق الموبايل**
  - React Native (iOS + Android)
  - Deep Linking
  - Offline Mode
- [ ] **برنامج الولاء المتقدم**
  - مستويات (Bronze, Silver, Gold)
  - مكافآت حصرية
  - نقاط الإحالة (Referral)
- [ ] **نظام التوصيات بالـ AI**
  - Collaborative Filtering
  - "منتجات قد تعجبك"
  - "عملاء آخرون اشتروا أيضاً"
- [ ] **الاشتراكات الشهرية**
  - سلة ثابتة كل أسبوع
  - خصومات للمشتركين

### المرحلة 4: الابتكار (شهر 7-12)

- [ ] **الواقع المعزز (AR)**
  - تجربة المنتجات في مطبخك
  - معاينة الأحجام
- [ ] **البحث الصوتي**
  - "ابحث عن طماطم"
  - دعم اللهجة المصرية
- [ ] **Chatbot AI**
  - ردود تلقائية ذكية
  - استفسارات شائعة
- [ ] **برنامج المزارعين**
  - Farm to Table
  - علامات "من المزرعة مباشرة"
  - دعم المنتجات المحلية

### المرحلة 5: التوسع الإقليمي (سنة 2+)

- [ ] الإطلاق في السعودية، الإمارات
- [ ] دعم لغات إضافية (English, French)
- [ ] شراكات دولية
- [ ] فتح API للبائعين الخارجيين (Marketplace Model)

---

## مؤشرات الأداء الرئيسية (KPIs)

### مؤشرات الأعمال

| المقياس | الهدف الشهري | الهدف السنوي |
|---------|--------------|--------------|
| **إجمالي الطلبات** | 1,500 طلب | 18,000 طلب |
| **المبيعات** | 450,000 ج.م | 5.4M ج.م |
| **متوسط قيمة الطلب (AOV)** | 300 ج.م | - |
| **العملاء النشطين** | 1,000 عميل | 10,000 عميل |
| **معدل الاحتفاظ (Retention)** | 40% | 60% |
| **معدل الطلبات المتكررة** | 25% | 50% |

### مؤشرات التقنية

| المقياس | الهدف |
|---------|-------|
| **Uptime** | 99.5% |
| **Page Load Time** | < 2 ثانية |
| **API Response Time** | < 500ms |
| **Error Rate** | < 0.5% |
| **Lighthouse Score** | > 90 |

### مؤشرات تجربة المستخدم

| المقياس | الهدف |
|---------|-------|
| **معدل التحويل (Conversion)** | 3-5% |
| **معدل التخلي عن السلة** | < 70% |
| **رضا العملاء (CSAT)** | > 4.5/5 |
| **Net Promoter Score (NPS)** | > 50 |
| **وقت الاستجابة للدردشة** | < 2 دقيقة |

---

## الملحقات

### المصطلحات التقنية

| المصطلح | الشرح |
|---------|--------|
| **RTL** | Right-to-Left (الكتابة من اليمين لليسار) |
| **JWT** | JSON Web Token (توكن المصادقة) |
| **CRUD** | Create, Read, Update, Delete |
| **API** | Application Programming Interface |
| **WebSocket** | بروتوكول اتصال ثنائي الاتجاه |
| **PWA** | Progressive Web App |
| **SSR** | Server-Side Rendering |

### روابط مفيدة

- **GitHub Repo**: [Internal - Not Public]
- **Figma Design**: [To be created]
- **API Documentation**: [To be created - Swagger/Postman]
- **Admin Panel**: `/admin`
- **Supabase Dashboard**: https://supabase.com/dashboard

---

## الخلاصة

**Lumina Fresh Market (Aloosh Market)** هو مشروع طموح يهدف لتحويل تجربة التسوق الغذائي في مصر. المشروع مبني على أساس تقني قوي (React + TypeScript + PostgreSQL) ويوفر ميزات حديثة مثل:

✅ **نظام طلبات متكامل** مع دعم متعدد الفروع
✅ **دردشة مباشرة** بتقنية WebSocket
✅ **لوحة تحكم إدارية** شاملة
✅ **تجربة مستخدم عربية** أصيلة 100%
✅ **قابلية توسع** لآلاف المستخدمين

**التحديات الرئيسية:**
- 🔴 معالجة الثغرات الأمنية (credentials leak)
- 🟡 تكامل الدفع الإلكتروني
- 🟡 بناء الثقة مع العملاء الأوائل

**النجاح يعتمد على:**
- سرعة التنفيذ والإطلاق
- التركيز على تجربة المستخدم
- التسويق الفعّال والشراكات الاستراتيجية

---

**تاريخ الإنشاء:** نوفمبر 2025
**آخر تحديث:** نوفمبر 2025
**الإصدار:** 1.0
**الحالة:** Development - Active

---

<div dir="rtl" style="text-align: center; padding: 20px; background: linear-gradient(135deg, #23110C 0%, #F97316 100%); color: white; border-radius: 10px;">
  <h2>🚀 جاهزون للانطلاق نحو المستقبل</h2>
  <p>Lumina Fresh Market - حيث الجودة تلتقي بالسرعة</p>
</div>
