# نظام العناوين المحفوظة 📍

## نظرة عامة
تم إضافة نظام "بيلينج ادريس" (العناوين المحفوظة) للموقع. الآن المستخدم يمكنه:
- حفظ عناوين متعددة
- اختيار عنوان محفوظ أثناء الشراء
- رؤية رسالة "هوصلك علي نفس العنوان؟"

## كيف يعمل؟

### 1. حفظ العنوان الأول
```
الملف الشخصي → عناويني → + إضافة عنوان
```
املأ البيانات:
- التسمية (منزل، عمل، آخر)
- الاسم الكامل
- رقم الهاتف
- المحافظة، المدينة، المنطقة
- الشارع، البناية، الطابق، الشقة
- معلم مميز (اختياري)
- ملاحظات للتوصيل (اختياري)
- ☑️ جعله افتراضي

### 2. الشراء باستخدام عنوان محفوظ
```
السلة → إتمام الطلب
```
- سترى قسم "العناوين المحفوظة"
- اضغط على العنوان المطلوب
- الحقول ستملأ تلقائياً
- أكمل الطلب

### 3. إدارة العناوين
```
الملف الشخصي → عناويني
```
يمكنك:
- ✏️ تعديل عنوان
- 🗑️ حذف عنوان
- ⭐ تعيين كافتراضي
- ➕ إضافة عنوان جديد

## الميزات

### ✨ Auto-Select للعنوان الافتراضي
العنوان المعيّن كافتراضي يُختار تلقائياً في الشراء

### 🎨 UI جميل
- بطاقات للعناوين مع أيقونات
- تمييز العنوان المختار
- Badge للعنوان الافتراضي
- تصميم responsive

### 🔒 أمان
- كل المستخدم يرى عناوينه فقط
- APIs محمية بـ JWT
- Validation للبيانات

### ⚡ Performance
- Loading states
- Error handling
- Smooth transitions

## التقنيات المستخدمة

### Frontend
- React + TypeScript
- Tailwind CSS
- Lucide Icons
- React Router

### Backend
- Express.js
- PostgreSQL
- JWT Authentication

### Database
```sql
addresses table:
- 17 حقل لتفاصيل العنوان الكامل
- is_default flag
- Foreign key إلى users
- Unique constraint (عنوان افتراضي واحد فقط)
```

## API Endpoints

```
GET    /api/addresses              - جلب جميع العناوين
GET    /api/addresses/:id          - جلب عنوان محدد
POST   /api/addresses              - إنشاء عنوان جديد
PUT    /api/addresses/:id          - تحديث عنوان
DELETE /api/addresses/:id          - حذف عنوان
POST   /api/addresses/:id/set-default - تعيين كافتراضي
```

## Files Structure

```
components/
  └── SavedAddressSelector.tsx    # اختيار العنوان في الشراء

pages/
  ├── CheckoutPage.tsx            # دمج مع الشراء
  ├── AddressesPage.tsx           # إدارة العناوين
  └── ProfilePage.tsx             # رابط إلى العناوين

server/routes/
  └── addresses.js                # APIs للعناوين

supabase/migrations/
  └── add_user_profile_fields.sql # Database schema
```

## UX Flow

```
1. User → Profile → "عناويني"
2. Add Address → Fill form → Save
3. Go to Checkout
4. See saved addresses
5. Click address → Auto-fill
6. Complete order
```

## Benefits

### للمستخدم
- ⚡ شراء أسرع
- 💾 حفظ عناوين متعددة
- ✅ لا حاجة لإدخال العنوان كل مرة
- 🏠 عناوين لأماكن مختلفة (منزل، عمل...)

### للموقع
- 📈 تحسين Conversion Rate
- 😊 تجربة مستخدم أفضل
- 📊 بيانات عناوين دقيقة
- 🚚 تسهيل عملية التوصيل

## Future Enhancements

- [ ] Google Maps Integration
- [ ] Address validation
- [ ] Delivery time estimation
- [ ] Address history
- [ ] Favorite addresses
- [ ] Share address link

## Support

إذا واجهت أي مشكلة:
1. تحقق من Console errors
2. تأكد من تسجيل الدخول
3. تأكد من تشغيل SQL migration
4. تحقق من Backend logs

---

**🎉 الآن المستخدمين يمكنهم حفظ عناوينهم واستخدامها في الطلبات القادمة!**
