# إعداد قاعدة البيانات - Database Setup

## المشكلة
عند إضافة منتج جديد في صفحة الأدمن، قوائم الفروع والتصنيفات كانت فارغة أو تعرض بيانات hardcoded.

## الحل
تم ربط القوائم المنسدلة بقاعدة البيانات بدلاً من البيانات الثابتة.

---

## 🔧 التعديلات المطبقة

### 1. تحديث Frontend (ProductsManager.tsx)
- إضافة `useState` للفروع والتصنيفات
- إضافة `loadBranches()` و `loadCategories()` functions
- استبدال القوائم الثابتة بـ dropdowns ديناميكية
- إضافة أزرار Seed للإضافة السريعة للبيانات

### 2. تحديث Backend Routes
#### `server/routes/branches.js`
- إضافة endpoint: `POST /api/branches/dev/seed`
- يضيف 3 فروع افتراضية (الفرع الرئيسي، المهندسين، مدينة نصر)

#### `server/routes/categories.js`
- إضافة endpoint: `POST /api/categories/dev/seed`
- يضيف 8 تصنيفات رئيسية
- يضيف 12 تصنيف فرعي

### 3. تحديث API Service (services/api.ts)
- تحسين error handling في `branches.getAll()`
- تحسين error handling في `categories.getAll()`
- معالجة response format بشكل صحيح

---

## 🚀 كيفية الاستخدام

### الطريقة الأولى: عبر UI
1. افتح صفحة Admin Products: `/admin/products`
2. اضغط على زر **"إضافة فروع"** (بنفسجي)
3. اضغط على زر **"إضافة تصنيفات"** (أزرق)
4. سيتم ملء قاعدة البيانات بالبيانات الافتراضية

### الطريقة الثانية: عبر API مباشرة

#### إضافة الفروع:
```bash
curl -X POST https://bodeelezaby-backend-test.hf.space/api/branches/dev/seed
```

#### إضافة التصنيفات:
```bash
curl -X POST https://bodeelezaby-backend-test.hf.space/api/categories/dev/seed
```

---

## 📊 البيانات الافتراضية

### الفروع (Branches)
1. **الفرع الرئيسي** - المعادي، القاهرة
2. **فرع المهندسين** - المهندسين، الجيزة
3. **فرع مدينة نصر** - مدينة نصر، القاهرة

### التصنيفات الرئيسية (Categories)
1. بقالة 🛒
2. ألبان 🥛
3. مشروبات 🥤
4. سناكس 🍿
5. حلويات 🍫
6. زيوت 🫗
7. منظفات 🧼
8. عناية شخصية 🧴

### التصنيفات الفرعية (Subcategories)
- **بقالة**: أرز، مكرونة، سكر
- **ألبان**: لبن، جبن، زبادي
- **مشروبات**: مشروبات غازية، عصائر
- **سناكس**: شيبس، بسكويت
- **حلويات**: شوكولاتة، حلوى

---

## ✅ التحقق من نجاح العملية

بعد تشغيل seed endpoints:

1. افتح Console في المتصفح (F12)
2. ابحث عن:
   ```
   📍 Branches loaded: [...]
   🗂️ Categories loaded: [...]
   ```

3. جرب إضافة منتج جديد:
   - يجب أن تظهر الفروع في القائمة المنسدلة
   - يجب أن تظهر التصنيفات في القائمة المنسدلة
   - عند اختيار تصنيف رئيسي، تظهر التصنيفات الفرعية

---

## 🐛 استكشاف الأخطاء

### القوائم لا تزال فارغة؟
1. تأكد من أن قاعدة البيانات متصلة (Supabase)
2. تحقق من Console للأخطاء
3. جرب seed endpoints مرة أخرى
4. تحقق من أن Backend deployed بالتعديلات الجديدة

### رسالة خطأ في API؟
```javascript
// في Console، شغل:
console.log(await api.branches.getAll())
console.log(await api.categories.getAll())
```

### التصنيفات الفرعية لا تظهر؟
- تأكد من اختيار تصنيف رئيسي أولاً
- التصنيفات الفرعية مرتبطة بـ `parent_id`

---

## 📝 ملاحظات مهمة

1. **Seed endpoints** للتطوير فقط (يبدأ بـ `/dev/`)
2. البيانات لن تتكرر - يتحقق من الوجود قبل الإضافة
3. يمكن إضافة المزيد من الفروع/التصنيفات عبر Admin UI
4. التصنيفات تدعم Hierarchy (رئيسي → فرعي)

---

## 🔗 الملفات المتأثرة

```
Frontend:
- pages/admin/ProductsManager.tsx

Backend:
- server/routes/branches.js
- server/routes/categories.js

API:
- services/api.ts
```

---

## 📞 Support

إذا واجهتك أي مشاكل، تحقق من:
1. Vercel Deployment Logs
2. Browser Console (F12)
3. Network Tab للـ API responses
