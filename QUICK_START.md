# خطوات تشغيل التحديثات

## 1. تطبيق قاعدة البيانات

### افتح Supabase SQL Editor:
1. اذهب إلى: https://supabase.com/dashboard
2. اختر مشروعك
3. من القائمة الجانبية: **SQL Editor**
4. انسخ محتوى الملف `database_migrations/loyalty_addresses.sql`
5. الصقه في المحرر واضغط **Run**

### أو استخدم Supabase CLI:
```bash
supabase db push database_migrations/loyalty_addresses.sql
```

## 2. إعادة تشغيل Backend

```bash
cd server
npm run dev
```

## 3. إعادة تشغيل Frontend

```bash
npm run dev
```

## التحقق من التحديثات

### اختبر الروابط من صفحة "المزيد":
- [x] طلباتي → `/my-orders` ✅
- [x] نقاطي → `/loyalty` ✅  
- [x] العناوين → `/addresses` ✅
- [x] فروعنا → `/branches` ✅
- [x] إرسال الاقتراح → `/chat` ✅
- [x] طلب المساعدة → `/chat` ✅
- [x] بطاقات الدفع → **محذوف** ✅

### اختبر نظام النقاط:
1. اعمل طلب جديد
2. وزع الطلب على ديليفري من لوحة التحكم
3. غير حالة الطلب إلى "تم التوصيل" من تطبيق الديليفري
4. افتح صفحة "نقاطي" → يجب أن ترى النقاط مضافة

---

**كل شيء جاهز! 🚀**
